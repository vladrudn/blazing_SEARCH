# Blazing Search

Швидкий пошуковий двигун для документів у мережевих папках.

## Огляд

Blazing Search - це високопродуктивний пошуковий додаток, призначений для індексації та пошуку документів у мережевих папках (зокрема, наказів та інших документів). Додаток забезпечує швидкий пошук за ключовими словами та контекстом у великих колекціях документів.

## Особливості

- 🚀 Швидкий пошуковий двигун
- 📁 Підтримка мережевих папок (SMB)
- 🔄 Автоматичне оновлення індексу
- 🌐 Веб-інтерфейс для зручного пошуку
- 📊 Підтримка індексації DOCX файлів
- 🛡️ Безпечний доступ до файлів

## Встановлення за допомогою Docker

### Швидкий запуск

1. Переконайтеся, що у вас встановлено Docker та Docker Compose
2. Змонтуйте SMB-ресурс на вашій системі
3. Скопіюйте `config.example.toml` в `config.toml` та налаштуйте шляхи
4. Запустіть:

```bash
./build-and-run.sh
```

Додаток буде доступний за адресою: http://localhost:8080

### Ручне встановлення

Дивіться детальні інструкції у файлі [quickstart.md](./quickstart.md).

## Налаштування

Додаток підтримує наступні способи налаштування:

1. **Файл конфігурації** (`config.toml`):
   ```toml
   [paths]
   remote_folder_path = "/шлях/до/мережевої/папки/Накази"
   photo_folder_path = "/шлях/до/мережевої/папки/ФОТО ВК"
   local_cache_path = "./nakazi_cache"
   documents_index_path = "documents_index.json"
   inverted_index_path = "inverted_index.json"
   ```

2. **Змінні середовища**:
   - `REMOTE_FOLDER_PATH`
   - `PHOTO_FOLDER_PATH`
   - `LOCAL_CACHE_PATH`
   - `DOCUMENTS_INDEX_PATH`
   - `INVERTED_INDEX_PATH`

## Використання

Після запуску веб-інтерфейс доступний за адресою `http://localhost:8080`. Введіть пошуковий запит у поле та натисніть Enter. Додаток автоматично індексує документи із зазначених папок і повертає результати пошуку.

## Технології

- Rust - основна мова програмування
- Actix-web - веб-фреймворк
- TOML - формат конфігурації
- Docker - контейнеризація
- CIFS/SMB - доступ до мережевих папок

## Підтримка

Для питань та підтримки звертайтесь до файлу [quickstart.md](./quickstart.md) або звертайтесь до автора проєкту.